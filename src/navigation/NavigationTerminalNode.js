export default class NavigationTerminalNode {

    constructor(url) {
        this.url = url; // string
    }
  
    // Getters/Setters
  
    getUrl() {
        return this.url;
    }

    setUrl(url) {
        this.url = url;
    }
  
  
  
  }
  